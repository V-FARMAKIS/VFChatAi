# VFChatAI Environment Configuration
# Copy this file to .env and fill in your actual values

# === REQUIRED: Email Configuration (for 2FA verification) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# === REQUIRED: AI Configuration ===
GEMINI_API_KEY=your-gemini-api-key-here

# === OPTIONAL: Database Configuration ===
# (Defaults are set for local PostgreSQL)
DB_USERNAME=postgres
DB_PASSWORD=admin
DB_URL=jdbc:postgresql://localhost:5432/postgres

# === How to get your credentials ===

# Gmail App Password:
# 1. Enable 2FA on your Gmail account
# 2. Go to: Google Account > Security > 2-Step Verification
# 3. Scroll down and click "App passwords"
# 4. Generate password for "VFChatAI" 
# 5. Use the 16-character password (not your regular Gmail password)

# Gemini API Key:
# 1. Go to: https://makersuite.google.com/app/apikey
# 2. Click "Create API Key"
# 3. Copy the generated key

# Note: Never commit this file with real credentials to version control!
